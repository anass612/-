# نظام إدارة الأصول (Asset Lifecycle Management) — المواصفة التقنية

## 1. تعريف المشكلة الحقيقية
هذا **ليس** نظام مخزون تجاري (شراء/بيع). هذا نظام تتبع **دورة حياة أصل مادي** لكل جهاز على حدة، بالرقم التسلسلي، من لحظة الشراء حتى الإحالة للتقاعد (retirement). الوحدة الأساسية هنا هي **الجهاز الفردي (asset instance)**، مو SKU بكمية.

الفرق مهم لأنه يغيّر تصميم قاعدة البيانات بالكامل:
- نظام مخزون تقليدي: `product_id` + `quantity`
- نظام أصول: كل جهاز له سجل مستقل، وله **تاريخ حياة كامل** (أين كان، عند مين، متى، وليش)

## 2. الكيانات الأساسية (Core Entities)

### 2.1 Asset (الجهاز)
```
- id (PK)
- serial_number (unique, indexed) — الرقم الداخلي اللي يعطيه نظامكم بعد التجميع
- manufacturer_serial_number (nullable, unique, indexed) — الرقم الأصلي من الشركة الأم على الكرتونة، **يُحفظ دائماً ولا يُستبدل أبداً**
- parent_asset_id (nullable, FK -> Asset) — لو هذا الأصل هو مكوّن فردي مركّب داخل جهاز أكبر (مثال أدناه)
- asset_type (camera / NVR / IoT-sensor / gateway / main_board / ...)
- model
- manufacturer
- purchase_date
- purchase_cost
- warranty_expiry — **مرتبط بـ manufacturer_serial_number**، مو بالرقم الداخلي
- current_status: [in_warehouse, deployed, in_maintenance, in_transit, retired, lost/damaged]
- current_location_id (FK -> Location, nullable if deployed)
- current_client_id (FK -> Client, nullable if in warehouse)
- condition_grade: [new, good, needs_repair, damaged]
- created_at, updated_at
```

**ليش فيه رقمين سيريال:** الجهاز يجيكم بكرتونة من الشركة الأم برقم تسلسلي خاص بيها (`manufacturer_serial_number`) — هذا الرقم هو مرجع أي مطالبة ضمان معهم مستقبلاً، ولازم يبقى ثابت للأبد. بعدين لما تجمّعونه مع باقي القطع وتضيفون نظامكم، يتولّد رقم تسلسلي داخلي جديد (`serial_number`) هو اللي يُستخدم بكل شاشات النظام والتتبع اليومي. **القاعدة:** لا تفقد الربط بينهم أبداً — إذا احتجت تسوي مطالبة ضمان، تدور بالرقم الداخلي وتطلع لك الرقم الأصلي مباشرة.

### 2.2 Client / Branch (العميل والفرع)
```
- id (PK)
- client_name
- branch_name
- branch_address
- external_crm_id (نص الحقل اللي يربطها بـ RIME CRM لاحقاً)
- contact_person
- status: [active, inactive]
```

### 2.3 Deployment (سجل النشر — الأهم في النظام)
هذا الجدول هو قلب النظام. كل مرة جهاز ينتقل من المستودع لعميل، أو يرجع، يتسجل هنا سطر.
```
- id (PK)
- asset_id (FK)
- client_id (FK)
- contract_reference (نص أو رقم، يربط لاحقاً بعقد الاشتراك في CRM)
- deployed_at
- expected_return_date (من تاريخ انتهاء الاشتراك)
- actual_return_date (nullable حتى يرجع فعلياً)
- deployed_by (user_id)
- returned_by (user_id, nullable)
- return_condition: [good, needs_repair, damaged, missing]
- notes
```

### 2.3.1 تاريخ الاستخدام التراكمي (Usage History) — مهم جداً

كل جهاز الآن يحتاج "سيرة ذاتية" كاملة، مو بس آخر حالة. هذا **مو جدول جديد** — هو استعلام (view/report) مبني فوق جدول Deployment نفسه، لأن كل سطر فيه أصلاً يحتوي التاريخ والعميل والمدة. لكن نحتاج نضيف عمود محسوب ونبني الشاشة اللي تعرضه:

```sql
-- عمود محسوب على كل سطر Deployment
duration_days = actual_return_date - deployed_at
   (أو current_date - deployed_at لو لسه deployed)
```

**شاشة "سيرة الجهاز" (Asset History View) — لكل جهاز:**
| رقم النشر | العميل | من تاريخ | إلى تاريخ | المدة (يوم) | حالة الإرجاع |
|---|---|---|---|---|---|
| ... | ... | ... | ... | ... | ... |

+ ملخص أعلى الشاشة: **عدد مرات التركيب الكلي**، **إجمالي أيام الاستخدام**، **متوسط مدة كل نشرة**، **آخر عميل استخدمه**.

**فايدة عملية حقيقية لهذا:** لو جهاز معين يرجع كل مرة بعد فترة قصيرة (مثلاً أقل من شهر بشكل متكرر)، هذا مؤشر إما جهاز معطوب باستمرار أو فيه مشكلة تركيب — تقدر تكتشفها بس من النظر لعدد مرات التركيب مقابل متوسط المدة، بدون ما حد يراجع يدوي.

### 2.4 Maintenance Log (سجل الصيانة/الفحص)
```
- id (PK)
- asset_id (FK)
- maintenance_type: [inspection, repair, firmware_update, cleaning]
- performed_at
- performed_by
- cost
- notes
- result: [passed, failed, needs_parts]
```

### 2.5 Location (المستودع)
مستودع واحد، مساحة مفتوحة واحدة لكل شيء (أجهزة رئيسية + قطع + مستهلكات مع بعض) — بدون تقسيم داخلي حالياً. نبقي الجدول بسيط لكن قابل للتوسع لاحقاً (لو احتجت تفصل منطقة فحص عن منطقة تخزين، أو تفتح مستودع ثاني):
```
- id (PK)
- name
- type: [warehouse] — قيمة وحيدة حالياً، الحقل جاهز لقيم إضافية لاحقاً بدون تعديل هيكلي
```

### 2.6 Users & Roles

بما إن المستخدمين أقل من 5 أشخاص، ما نحتاج نظام صلاحيات معقّد من اليوم الأول، لكن الفني له طبيعة مختلفة فعلاً (يحتاج حساب دائم بصلاحية محدودة، مو حساب مؤقت):
```
- Admin: صلاحية كاملة (على الأغلب أنت أو مدير العمليات)
- Warehouse Staff: كل عمليات المستودع (تجميع، تفكيك، استلام، نشر داخلي)
- Technician: حساب دائم، يشوف مهامه المُسندة له بس (Task list)، يفتح تفاصيل كل مهمة، يضغط "إنهاء" — ما يشوف باقي شاشات النظام (المخزون، الفواتير، التقارير)
```
**إنشاء المهام:** المدير (Admin) هو اللي ينشئ مهمة للفني يدوياً — لا يوجد جدولة تلقائية أو تقويم حالياً. الفني نفسه لا ينشئ مهام، بس يستلم ويُنهي.

نظام صلاحيات أدق أكثر من هذا (مثلاً Ops/Manager منفصل عن Admin) نأجله لمرحلة لاحقة لو كبر عدد المستخدمين — تصميم قاعدة البيانات (Row Level Security) يسمح بإضافته لاحقاً بدون إعادة هيكلة.

### 2.7 Asset Model / Catalog (كتالوج الموديلات) — جدول جديد لدعم إعادة الشراء

هذا **مختلف عن جدول Asset**. جدول Asset يمثل جهاز فردي واحد بسريال محدد. هذا الجدول الجديد يمثل "الموديل" ككل (مثلاً "كاميرا XYZ موديل ABC-100") — وبيصير مرجع لكل نسخة من نفس الموديل.

```
- id (PK)
- model_name
- category (camera / NVR / IoT-sensor / gateway / ...)
- preferred_supplier
- supplier_contact
- unit_cost (آخر سعر شراء معروف)
- lead_time_days (كم يوم ياخذ التوريد من وقت الطلب)
- reorder_threshold (الحد الأدنى — لو العدد المتوفر وصله، ينطلق تنبيه)
- reorder_quantity (الكمية المقترحة للطلب كل مرة)
- notes
```

**العدد المتوفر (Available Count) — ما يُخزَّن، يُحسَب لحظياً:**
```sql
SELECT model_name, COUNT(*) as available_count
FROM assets
WHERE model_id = X AND current_status = 'in_warehouse' AND condition_grade IN ('new', 'good')
GROUP BY model_id
```

**تنبيه إعادة الشراء:**
```
IF available_count <= reorder_threshold
   → تنبيه: "الموديل [X] وصل [Y] قطعة متبقية — الحد الأدنى [threshold]. مدة التوريد المعتادة [lead_time] يوم."
```

**قرار عملي مهم:** لا تحسب `available_count` باستخدام جهاز `in_maintenance` أو `damaged` كأنه "متوفر" — غلط شائع يخلي التنبيه يتأخر ويوصلك بعد فوات الأوان. العدد المتوفر الحقيقي هو بس اللي فعلاً جاهز للنشر فوراً.

## 2.8 نظام التصنيع الفرعي (BOM + تجميع + تفكيك) — إضافة جوهرية

هذا القسم يغيّر التصميم من "تتبع أصول بس" إلى **تتبع أصول + تصنيع مصغّر (light manufacturing)**. الجهاز الرئيسي ما يُشترى جاهز — يُجمّع من 6 قطع بالمستودع، ويُفكّك عند الاسترجاع. وفيه خدمة جديدة (حساسات) لها منطق شبيه لكن أبسط.

### 2.8.1 Component (القطع الخام)
```
- id (PK)
- component_name (مثلاً: "لوحة رئيسية", "كيبل داخلي", "غطاء بلاستيك", ...)
- category
- unit_cost
- quantity_on_hand (كمية عامة بالمخزن — bulk، ليست فردية)
- reorder_threshold
- reorder_quantity
- preferred_supplier
```

**استثناء مهم ومؤكد الآن:** القطعة اللي تجيكم جاهزة من الشركة الأم بكرتونة وسريال خاص (على الأغلب اللوحة الرئيسية/الجهاز الأساسي قبل التجميع) **لا تُتبع كـ Component بالكمية العامة** — تُسجَّل كـ **Asset مستقل** فور استلامها من الشراء (قبل حتى ما تنضم لجهازكم النهائي)، بحقل `manufacturer_serial_number` معبّى من الكرتونة مباشرة. باقي القطع الخمس (كيبلات، أغطية، إلخ) تبقى bulk عادي، إلا لو تكتشف قطعة ثانية لها سريال من مورد — نفس المنطق ينطبق عليها.

**كيف يرتبط هذا بالتجميع (2.8.3):** عند تجميع الجهاز النهائي، القطعة المُسريلة (اللوحة الرئيسية مثلاً) تصبح `parent_asset_id` = سريال الجهاز النهائي الجديد. يعني الجهاز النهائي "يحتوي" على أصل فرعي محتفظ برقمه الأصلي، وباقي 5 قطع bulk تُخصم من `quantity_on_hand` عادي.

### 2.8.2 Product Definition / BOM (تعريف المنتج وقائمة مكوناته)
```
- id (PK)
- product_name ("الجهاز الرئيسي - AI Box", "تركيب حساس حرارة", "تركيب حساس باب", ...)
- product_type: [main_device, sensor_install]
- bom_items: [{component_id, quantity_required}]   -- قائمة المكونات وكمية كل واحد
```
مثال: "الجهاز الرئيسي" = 6 سطور BOM (كل قطعة ×1، أو حسب الكمية الفعلية). "تركيب حساس حرارة" = سطر واحد (سنسور الحرارة) + بوابة Zigbee.

### 2.8.2.1 BOM الفعلي لجهاز RES — Product: "الجهاز الرئيسي RES"

| المكون | نوع التتبع | ملاحظة |
|---|---|---|
| **O-NA** (عقل الجهاز) | **Asset فردي** — `manufacturer_serial_number` إلزامي | العنصر الحرج، عليه كل الضمان |
| **SSD** | **Asset فردي** — `manufacturer_serial_number` إلزامي | ✅ مؤكد: ضمان مصنّع مرتبط بسريال خاص، نفس معاملة O-NA بالضبط |
| كفر (حديد/بلاستيك) | Component bulk | مجرد غلاف، بدون قيمة ضمان |
| CAT7 Ethernet Cable | Consumable bulk | كيبل توصيل، يُستهلك |
| Power Cable UK plug | Consumable bulk | سلك كهرباء، يُستهلك |
| Female-Female Jumper Wire (Pack of 4) | Component bulk | أسلاك توصيل داخلية |
| LED Light Module | Component bulk | وحدة إضاءة، غير حرجة |

**سؤال يحدد التصميم:** SSD — هل تحتاج تتتبع سريال كل قرص SSD فردياً (لأن أغلب الشركات المصنّعة تتطلب رقم السريال بالضبط لأي مطالبة ضمان)، أو تتعامل معه كقطعة استهلاكية عادية زي الكيبلات؟ إذا كان جوابك "نعم"، بيتصمم بالضبط زي O-NA (Asset فردي بـ `manufacturer_serial_number` + `parent_asset_id`).

**ملاحظة على العدد:** ذكرت "6 قطع" سابقاً لكن القائمة فعلياً 7 عناصر — ما فيه مشكلة، بس أنبّهك لو كان فيه قطعة سقطت سهواً من القائمة أو واحدة مو مستخدمة دايماً.

## 11. التقارير (Reports) — لوحة تحكم مدير المخزون

هذا القسم يفكّر فيه كأنك مدير المخزون فعلياً يفتح النظام كل صباح ونهاية كل شهر. كل تقرير مبني فوق الجداول اللي صممناها — ما يحتاج بنية جديدة، بس يحتاج views/queries محددة + شاشة تصدير.

| التقرير | مصدر البيانات | يجاوب على سؤال |
|---|---|---|
| **تقرير الهدر/التلف (Waste Report)** | `Disassembly Events` (قطع `condition=damaged`) + `Deployment` (`return_condition=damaged/missing`) | كم خسرنا هذا الشهر، أي قطعة تتلف أكثر، أي مورّد جودته ضعيفة |
| **تقرير التركيبات (Installations Report)** | `Task` (`task_type=installation`, `status=completed`) مع فلترة تاريخ/عميل/فني | كم تركيب صار بفترة معينة، مين الفني الأنشط، أي عميل يطلب أكثر |
| **تقرير مستويات المخزون (Stock Levels)** | `Component.quantity_on_hand` + `Consumables.quantity_on_hand` مقابل `reorder_threshold` | وش قاعد يخلص الحين، وش يحتاج طلب فوري |
| **تقرير المشتريات (Procurement Report)** | `Purchase Invoice` مجمّعة حسب مورّد/فترة | كم صرفنا هالربع، أي مورّد الأغلى، اتجاه الأسعار |
| **تقرير حالة الأصول (Asset Status Snapshot)** | `Asset` مجمّع حسب `current_status` | كم جهاز بالمستودع، عند عملاء، بالصيانة، الآن لحظياً |
| **تقرير انتهاء الضمان (Warranty Expiry)** | `Asset.warranty_expiry` (مرتبط بـ `manufacturer_serial_number`) قادم خلال 30/60/90 يوم | أي O-NA/SSD قارب ينتهي ضمانه، قبل ما تحتاجه بمطالبة |
| **تقرير أداء الفنيين** | `Task` مجمّع حسب `assigned_to`: عدد مكتمل، متأخر، متوسط وقت الإنجاز | مين الفني الأسرع/الأكثر إنتاجية |
| **سيرة الفروع (Branch History Report)** | `Task` مجمّع حسب `branch_id` (قسم 2.10) | كم زيارة لكل فرع تاريخياً، بغض النظر عن تغيّر الأجهزة |
| **تقرير التكلفة الإجمالية (TCO per Unit)** | `Assembly Event.total_component_cost` + `Maintenance Log.cost` مجمّعة لكل Asset | التكلفة الحقيقية الكاملة لكل جهاز عبر عمره |
| **تقرير بطاريات مستحقة (Battery Due)** | `Asset` حيث `battery_installed_at + expected_life` قارب ينتهي | جدولة زيارات استباقية قبل الشكاوى |

**تنسيق التصدير:** كل تقرير يُصدَّر **CSV** كخيار أساسي (يفتح بإكسل مباشرة، ولا يحتاج مكتبات تصميم). PDF للعرض بالاجتماعات نأجله لمرحلة لاحقة لو احتجته فعلاً — شكل، مو جوهر.

**الجدولة:** التقارير **عند الطلب فقط** (on-demand) — بدون جدولة تلقائية أو إرسال دوري بالإيميل. هذا يبسّط المرحلة 1 كثير: ما نحتاج بنية cron jobs أو مهام مجدولة للتقارير، بس endpoint/شاشة تشغّل الـ view وتصدّر النتيجة لحظياً. (ملاحظة: هذا مختلف عن **التنبيهات** بقسم 4 اللي لازم تكون استباقية/تلقائية — التقارير تحليلية تُطلب، والتنبيهات تحذيرية تُدفع).

**قرار معماري يخدم "البناء المستقبلي" اللي طلبته:** كل تقرير فوق **يُبنى كـ SQL VIEW في Postgres**، مو كود منطق داخل التطبيق. هذا يعني:
- أي أداة BI تربطها لاحقاً (Metabase، Looker، حتى Excel عبر ODBC) تقرأ من نفس الـ views مباشرة بدون إعادة كتابة منطق
- تقدر تضيف تقرير جديد بإضافة view جديد فقط، بدون تعديل كود التطبيق
- الأداء أفضل لأن Postgres يحسبها مباشرة، مو التطبيق يسحب كل البيانات ويحسبها بالذاكرة

### 2.8.3 Assembly Event (عملية التجميع)
```
- id (PK)
- product_id (FK -> Product Definition)
- assembled_by (user_id)  ← الشخص اللي جمّع الجهاز
- assembled_at
- resulting_asset_id (FK -> Asset)  ← السريال الداخلي الجديد الناتج
- source_manufacturer_asset_id (FK -> Asset، nullable)  ← القطعة المُسريلة من الشركة الأم (إن وجدت)، تُربط كـ parent عبر parent_asset_id
- components_consumed: [{component_id, quantity, unit_cost_at_time}]  ← القطع bulk فقط
- total_component_cost (محسوب: SUM(quantity × unit_cost_at_time) + تكلفة القطعة المُسريلة)
```
**عند التجميع:** يُخصم `quantity_on_hand` من كل Component bulk حسب BOM، ويُنشأ سطر جديد في جدول Asset (status = `in_warehouse`) بسريال داخلي جديد، ويُربط بالقطعة المُسريلة (لو وجدت) عبر `parent_asset_id`، ويُسجَّل `assembled_by`.

**عند الاستفسار عن الضمان لاحقاً:** تدور بالسريال الداخلي → تلقى `parent_asset_id` → تطلع `manufacturer_serial_number` مباشرة، بدون ما تحتاج تدور يدوي بسجلات قديمة.

### 2.8.4 Disassembly Event (عملية التفكيك) — عكس التجميع تماماً
```
- id (PK)
- asset_id (FK -> Asset الجهاز اللي يتفكك)
- disassembled_by (user_id)
- disassembled_at
- components_returned: [{component_id, quantity, condition: good/damaged}]
- notes
```
**عند التفكيك:** كل قطعة `condition = good` يرجع كميتها لـ `quantity_on_hand`. كل قطعة `condition = damaged` تُسجَّل كخسارة (write-off) في تقرير منفصل — **لا ترجع للمخزون المتاح**. الجهاز الأصلي (Asset) تنتقل حالته إلى `disassembled` (نوع جديد من retired).

**فايدة عملية:** بهذا التصميم تقدر تعرف بالضبط: كم قطعة "لوحة رئيسية" تتلف بالمعدل كل تفكيك — يفيدك تفاوض مع المورد لو نسبة التلف عالية بشكل غير طبيعي.

### 2.8.5 المستهلكات (Consumables) — كيبل، سلك كهرباء
```
- id (PK)
- consumable_name (كيبل شبكة، سلك كهرباء، ...)
- unit_cost
- quantity_on_hand
- reorder_threshold
```
تُستهلك عند كل عملية نشر (Deployment) عبر جدول ربط:
```
Deployment_Consumables: [{deployment_id, consumable_id, quantity_used, cost_at_time}]
```
لا ترجع أبداً للمخزون — تُخصم عند الاستخدام وخلاص، عكس القطع (Components) اللي ممكن ترجع من التفكيك.

### 2.8.6 الحساسات والبوابة (Zigbee Gateway)

**مؤكد:** الحساسات نفسها والبوابة تتبع نفس منطق الجهاز الرئيسي تماماً — أصل فردي (Asset)، يُنشر ويُسترجع، إلا لو صار فيه تلفيات (عندها تُسجَّل كـ `damaged` ولا ترجع للمخزون المتاح، بنفس منطق التفكيك في 2.8.4).

### 2.8.7 تتبع البطاريات (Battery Tracking) — حقول إضافية على Asset

بعض الحساسات تعمل ببطارية لها عمر افتراضي، وتنطفئ عند انتهائها، والبطارية قابلة للاستبدال. هذا يعني إن الحساس نفسه له "دورة حياة فرعية" مستقلة عن دورة حياة الجهاز (نشر/استرجاع):

```
إضافة على جدول Asset (تنطبق فقط لو asset_type = sensor و has_battery = true):
- has_battery (boolean)
- battery_installed_at
- battery_expected_life_days (nullable — لسه ما عندك هذا الرقم، تضيفه لاحقاً)
- battery_last_replaced_at (nullable — يتحدث عند كل استبدال)
```

**قرار عملي:** `battery_expected_life_days` حقل **اختياري (nullable) من اليوم الأول** — طالما ما عندك الرقم الفعلي، تنبيه البطارية ببساطة **يُتجاهل تلقائياً** لأي حساس ما له قيمة بهالحقل، بدون ما يعطّل باقي النظام. تعبيه لاحقاً حساس بحساس أو نوع بنوع وقت ما يتوفر عندك الرقم من التجربة الفعلية أو من المصنّع.

**استبدال البطارية = سطر جديد في Maintenance Log** بنوع `battery_replacement`، وعند تسجيله يُحدَّث `battery_installed_at` تلقائياً بتاريخ اليوم (يعني العد يبدأ من جديد).

**تنبيه استباقي — الأهم عملياً هنا:**
```
IF (current_date - battery_installed_at) >= (battery_expected_life_days - 14)
   → تنبيه: "بطارية الحساس [serial] عند العميل [X] بتخلص خلال أسبوعين تقريباً — جدول زيارة استبدال"
```

**ليش هذا التنبيه أهم من أي تنبيه ثاني بالنظام:** لو الحساس انطفى فعلاً، العميل هو اللي يكتشف ويشتكي — يعني تأخرت. التنبيه الاستباقي (قبل الانطفاء بأسبوعين) يخليك تجدول زيارة استباقية بدل ما تتفاجأ بشكوى عميل عن نظام "مو شغال".

**ملاحظة تصميم:** `battery_expected_life_days` يُحدَّد على مستوى Product Definition (نوع الحساس)، مو الجهاز نفسه — كل نوع حساس له عمر بطارية مختلف (حساس حرارة ممكن يختلف عن حساس باب)، فيُفترض يكون حقل ثابت بجدول Product Definition (2.8.2) ويُنسخ تلقائياً على Asset عند التجميع/التسجيل.

### 2.8.7 الفواتير (Invoices) — توثيق تكلفة داخلي (بدون بعد ضريبي)

بناءً على تأكيدك إنها للتوثيق الداخلي فقط:

**Purchase Invoice (فاتورة شراء من مورد):**
```
- id (PK)
- supplier_name
- invoice_number
- invoice_date
- line_items: [{component_id or consumable_id, quantity, unit_cost}]
- shipping_cost
- total_cost
- attached_file (رفع صورة/PDF الفاتورة الأصلية)
```

**Installation Cost Sheet (ملف تكلفة التركيب — مو فاتورة عميل، توثيق داخلي)**
```
- id (PK)
- deployment_id (FK)
- component_cost (من Assembly Event المرتبط)
- consumables_cost (من Deployment_Consumables)
- shipping_allocation (توزيع نسبي من تكلفة الشحن الكلية)
- labor_cost (اختياري)
- total_unit_cost
```
هذا يعطيك **التكلفة الحقيقية الكاملة لكل وحدة منشورة عند عميل** — شراء + شحن + تركيب — بدون ما تحتاج نظام محاسبة رسمي (ZATCA/فوترة ضريبية). لو احتجت فوترة ضريبية فعلية للعميل مستقبلاً، هذا نطاق منفصل تماماً (يحتاج ربط مع نظام محاسبي معتمد)، ونتعامل معه كمشروع مستقل وقتها.

## 2.9 واجهة الفني ونظام المهام (Technician Task System)

الفني يحتاج شاشة مختلفة تماماً عن شاشة المستودع — مهمة واحدة بكل تفاصيلها، مو جدول أجهزة. هذا يتطلب كيان جديد يربط كل شي:

### 2.9.1 Task / Work Order (مهمة الفني)
```
- id (PK)
- task_type: [installation, maintenance, battery_replacement, retrieval, inspection]
- branch_id (FK -> Client/Branch)
- assigned_to (technician user_id)
- scheduled_date
- status: [pending, in_progress, completed, cancelled]
- related_product_id (FK -> Product Definition — لو تركيب جديد، أي BOM/منتج يركّبه)
- related_asset_id (FK -> Asset, nullable — لو صيانة/استرجاع لجهاز موجود)
- checklist (نص أو قائمة: القطع/المستهلكات المطلوبة، تعليمات خاصة)
- completion_notes
- completed_at
```

**شاشة الفني (Task Detail View) تعرض كل ما يحتاجه بمكان واحد:**
- اسم العميل/الفرع + العنوان + رقم تواصل
- نوع المهمة وتفاصيلها (تركيب جديد؟ صيانة جهاز موجود؟ استبدال بطارية؟)
- قائمة القطع/المستهلكات المطلوبة (تُسحب تلقائياً من BOM لو تركيب)
- زر "إنهاء المهمة" — عند الضغط عليه، **يُنشئ تلقائياً** السجل المناسب في الخلفية (Deployment جديد لو تركيب، Maintenance Log لو صيانة، تحديث battery_installed_at لو استبدال بطارية) — الفني ما يدخل بيانات مكررة، يعبي نموذج إنهاء واحد بس

**قرار عملي:** لا تخلي الفني يتعامل مع جداول Asset/Deployment مباشرة — واجهته يجب تكون مبسطة لدرجة إنه يقدر يستخدمها بدون تدريب. كل التعقيد (الربط بالجداول الخلفية) يصير تلقائي عند "إنهاء المهمة".

## 2.10 تاريخ الفرع (Branch Visit History) — مستقل عن تغيّر السريال

هذي نقطة تصميم دقيقة ومهمة: لو جهاز عند فرع معين تغيّر (تبدّل، تحديث، استبدال) 3 مرات خلال سنتين، **تاريخ الفرع نفسه يجب يبقى متصل ومستمر** — مو مقسّم حسب كل سريال جهاز.

الحل: تاريخ الفرع **ما يُبنى من جدول Asset**، يُبنى من جدول **Task** (2.9.1) نفسه، لأن `branch_id` موجود على كل مهمة بغض النظر عن أي جهاز تغيّر:

```sql
SELECT branch_id, COUNT(*) as total_visits, task_type, scheduled_date
FROM tasks
WHERE branch_id = X AND status = 'completed'
ORDER BY scheduled_date DESC
```

**شاشة "سيرة الفرع" تعرض:**
- إجمالي عدد الزيارات (كل الأنواع)
- تفصيل حسب نوع (كم تركيب، كم صيانة، كم استبدال بطارية)
- كل جهاز مرّ على هذا الفرع تاريخياً (حتى لو ما هو موجود فيه حالياً) — عبر ربط `Deployment.client_id` بغض النظر عن `asset_id`

هذا التصميم يحل المشكلة اللي ذكرتها بالضبط: تاريخ الفرع يبقى واحد متصل، والأجهزة تجي وتروح فوقه بدون ما تكسر الاستمرارية.

## 3. الحالات وانتقالاتها (State Machine)

```
in_warehouse ──(deploy)──> deployed
deployed ──(retrieve)──> in_transit
in_transit ──(receive at warehouse)──> in_maintenance
in_maintenance ──(pass QC)──> in_warehouse
in_maintenance ──(fail QC)──> retired / needs_repair
in_warehouse ──(write-off)──> retired
deployed ──(reported lost/damaged)──> lost/damaged
```

**قاعدة صارمة:** أي انتقال حالة يجب أن يمر عبر سطر جديد في جدول Deployment أو Maintenance Log — لا يوجد تحديث مباشر لـ `current_status` بدون أثر (audit trail). هذا ضروري لأنك تتعامل مع أصول عميلك (RIME) تحت عهدة، وأي نزاع مستقبلي (جهاز مفقود، تلف) يحتاج سجل زمني موثّق.

## 4. التنبيهات الحرجة (Alerts) — هذي أهم ميزة عملية بالنظام

| التنبيه | الشرط | الأولوية |
|---|---|---|
| اقتراب انتهاء اشتراك | `expected_return_date` خلال 14 يوم ولا يوجد `actual_return_date` | عالية |
| تأخر استرجاع | تجاوز `expected_return_date` بدون استرجاع | حرجة |
| جهاز واقف بدون حركة | `in_maintenance` لأكثر من X يوم | متوسطة |
| انتهاء ضمان قريب | `warranty_expiry` خلال 30 يوم والجهاز `deployed` | منخفضة |
| **إعادة شراء (Reorder)** | `available_count` لموديل معين ≤ `reorder_threshold` | عالية |
| جهاز يرجع بشكل متكرر بمدة قصيرة | عدد deployments لنفس الجهاز خلال 6 أشهر > 3 ومتوسط المدة < 30 يوم | متوسطة (فحص جودة) |
| **بطارية حساس قاربت الانتهاء** | `(current_date - battery_installed_at) >= (battery_expected_life_days - 14)` | **عالية — استباقي، قبل ما يشتكي العميل** |

بدون هذي التنبيهات، النظام مجرد سجل بيانات — القيمة العملية الحقيقية هي إنه يمنعك تنسى تسترجع جهاز أو يفوتك موعد.

## 5. التكامل مع RIME CRM (لاحقاً — عبر API)

بما إنه "نظام مستقل يتكامل لاحقاً"، نصممه من الآن بحيث:
- `external_crm_id` في جدول Client موجود من اليوم الأول (فارغ حالياً، يتعبى لاحقاً)
- `contract_reference` في Deployment نفس الشيء
- التكامل الفعلي يكون **read-only pull**: نظام الأصول يسحب بيانات العميل/العقد من CRM دوري (webhook أو polling كل ساعة)، ما يكتب فيه
- هذا يحميك من تضارب مصدر الحقيقة (source of truth): CRM يبقى مصدر بيانات العميل والعقد، ونظامك يبقى مصدر بيانات الأصل وموقعه

**تحذير عملي:** لا تبني التكامل الآن قبل ما تعرف شكل API الفعلي لـ RIME CRM (endpoints، auth method، rate limits). ابني النظام بحقول جاهزة للربط، واترك التكامل الفعلي لمرحلة لاحقة بعد ما تتأكد من التوثيق (docs) أو تحصل access.

## 5.5 استراتيجية الترحيل من Airtable

**المعطيات:** البيانات الحالية (500-2000 جهاز) موجودة في Airtable، وسيستمر استخدامه بالتوازي فترة انتقالية.

**القرار الحاسم: مصدر حقيقة واحد فقط (Single Source of Truth)**

تشغيل نظامين بالتوازي كمصدرين للتحديث معناه تضارب بيانات مضمون — شخص يحدّث حالة جهاز في Airtable، وشخص ثاني يسجّل نفس الجهاز بحالة مختلفة في النظام الجديد، ولا أحد يعرف مين الصحيح. لتفادي هذا:

1. **الاستيراد الأولي (One-time Import):** سحب كامل البيانات من Airtable عبر الـ API (متاح عندك كـ connector) إلى قاعدة بيانات النظام الجديد، مرة واحدة، بتاريخ محدد يُعتبر "لحظة القطع" (cutover point)
2. **بعد لحظة القطع:** كل التحديثات تصير في النظام الجديد فقط. Airtable يتحول لأرشيف قراءة فقط (read-only reference) لأي شخص يحتاج يرجع لسجل قديم
3. **لا مزامنة مستمرة بالاتجاهين** — لأنها تتطلب بنية conflict-resolution معقدة لا تستاهل التعقيد لحجم بيانات 500-2000 جهاز ومستخدمين أقل من 5

**قبل الاستيراد، لازم نعرف:**
- أسماء الحقول (columns) الفعلية في Airtable وكيف تُخزَّن الحالة الحالية لكل جهاز (نص حر؟ dropdown محدد؟)
- هل فيه حقول ناقصة عن الكيانات المطلوبة (مثلاً تاريخ الشراء، الضمان) — إذا ناقصة، نستوردها كـ null ونكمّلها لاحقاً، ما نوقف المشروع لأجلها
- هل الأرقام التسلسلية (serial numbers) مُدخلة بشكل موحّد وما فيها تكرار — هذا أخطر نقطة، لأن `serial_number` هو المفتاح الفريد لكل الأصل

## 6. التقنيات المقترحة (Tech Stack)

| الطبقة | الخيار المقترح | السبب |
|---|---|---|
| قاعدة البيانات | Supabase (Postgres) | متصل عندك كـ connector، فيه Auth جاهز، فيه Realtime لو احتجت تحديثات لحظية |
| Backend logic | Supabase Edge Functions أو API layer بسيط | يقلل البنية التحتية المطلوبة |
| Frontend | Web app (React) | متاح عندك Netlify للنشر مباشرة |
| Auth/Roles | Supabase Auth + Row Level Security | يفرض الصلاحيات على مستوى قاعدة البيانات، مو بس الواجهة |
| Barcode/QR | لاحقاً — مكتبة JS للمسح من كاميرا الموبايل، بدون هاردوير إضافي | ما ذكرتها كأولوية، نأجلها لمرحلة 2 |

**قرار عملي:** ما أنصح تبدأ بتطبيق موبايل منفصل من اليوم الأول. ابدأ بـ web app واحد responsive يشتغل على الموبايل من المتصفح — يقلل وقت التطوير للنصف، وتقدر تحوله لتطبيق حقيقي لاحقاً لو احتجت offline mode فعلي.

## 7. خطة التنفيذ المرحلية (محدّثة بعد إضافة BOM/التصنيع)

**المرحلة 1 — الأساس (MVP) — أكبر من قبل، لأن التجميع/التفكيك جوهر عملكم مو إضافة اختيارية**
- جداول Asset, Client, Deployment, Location
- جداول Component, Product Definition (BOM), Assembly Event, Disassembly Event
- جداول Consumables, Deployment_Consumables
- **استيراد أولي من Airtable** (لحظة قطع محددة، راجع قسم 5.5)
- عمليات: تجميع جهاز جديد (يخصم قطع، ينشئ Asset)، نشر جهاز، استرجاع + تفكيك جهاز (يرجع قطع سليمة، يسجل تالفة)
- بحث سريع بالسريال/العميل/الحالة (إلزامي من اليوم الأول)
- شاشة واحدة: قائمة كل الأجهزة بحالتها الحالية
- Auth بسيط: Admin + Warehouse Staff فقط
- قناة تنبيه فعلية واحدة (إيميل على الأقل) تشتغل من أول يوم

**المرحلة 2 — واجهة الفني، الفواتير والتكلفة + التنبيهات المتقدمة**
- **جدول Task/Work Order + شاشة الفني المبسطة** (قسم 2.9) — أولوية عالية، لأنه يربط النظام بالعمل الفعلي اليومي
- **تاريخ الفرع المستقل عن السريال** (قسم 2.10)
- Purchase Invoice + Installation Cost Sheet
- تنبيه إعادة شراء لكل Component و Consumable (مو بس الأجهزة الجاهزة)
- تنبيهات انتهاء الاشتراك والتأخر
- تقرير: كم جهاز عند كل عميل، كم جهاز في المستودع، كم في الصيانة
- عملية نشر/استرجاع جماعية (Bulk) — لأن التركيبات غالباً دفعة كاملة

**المرحلة 3 — الصلاحيات الموسّعة والتدقيق والتحليلات**
- Audit trail كامل لكل حركة (إن لم يكن مبني من المرحلة 1)
- تتبع التكلفة الإجمالية (TCO) لكل جهاز عبر عمره الكامل
- تقرير نسبة التلف لكل قطعة (من Disassembly Events) — مؤشر جودة المورّد
- توسيع الأدوار لو زاد عدد المستخدمين

**المرحلة 4 — التوثيق الرقمي للعهدة والتكامل**
- PDF عهدة يتولد تلقائياً عند كل عملية نشر (بدل الورق الحالي)
- ربط RIME CRM الفعلي عبر API
- QR/Barcode scanning
- دعم أكثر من مستودع (البنية جاهزة له من المرحلة 1)

**ملاحظة عملية:** توثيق العهدة الرقمي (توليد PDF أو توقيع إلكتروني) مؤجل بناءً على قرارك، لكن انتبه — طول ما هو ورقي، فيه فجوة بين "النظام يقول الجهاز عند عميل X" و"يوجد إثبات موقّع فعلي لذلك". لو صار نزاع مع عميل قبل ما توصل للمرحلة 4، ما عندك حماية رقمية. قرار معقول للأولويات الحالية، بس خله بالحسبان.

## 8. ملخص القرارات المحسومة

| القرار | الاختيار |
|---|---|
| نوع النظام | Asset Lifecycle Management (مو مخزون تقليدي) |
| حجم البيانات | 500-2000 جهاز |
| مصدر البيانات الحالي | Airtable |
| استراتيجية الترحيل | استيراد أولي مرة واحدة + لحظة قطع، لا مزامنة مستمرة |
| التكامل مع RIME CRM | لاحقاً (مرحلة 4)، عبر API، read-only pull |
| توثيق العهدة | ورقي حالياً، رقمي بمرحلة 4 |
| عدد المستخدمين | أقل من 5 |
| الصلاحيات | Admin + Warehouse Staff فقط (تبسيط متعمّد) |
| الواجهة | Web app واحد responsive (بدون تطبيق موبايل منفصل حالياً) |
| قاعدة البيانات | Supabase (Postgres) |
| النشر | Netlify |
| القطع الخام (6 قطع الجهاز) | تُتبع بالكمية العامة (bulk)، بدون سيريال فردي |
| البوابة (Zigbee gateway) | أصل فردي (Asset)، تُنشر وتُسترجع مثل الجهاز الرئيسي بالضبط |
| التفكيك عند الاسترجاع | قطع سليمة ترجع للمخزون، قطع تالفة تُسجَّل كخسارة ولا ترجع |
| المستهلكات (كيبل، سلك كهرباء) | تُخصم عند الاستخدام، لا ترجع أبداً |
| الفواتير | توثيق تكلفة داخلي فقط، بدون بعد محاسبي/ضريبي رسمي (لا ZATCA) |

## 9.5 ميزات إضافية من واقع التطبيق (لم تُذكر صراحة) — قرر وش يدخل ووش يتأجل

هذي ما اخترعتها نظرياً — هذي أشياء أول ما تشتغل بنظام أصول حقيقي بتحتاجها خلال أول 3 أشهر وتتمنى لو كانت من البداية:

| الميزة | ليش تحتاجها فعلياً | أولوية مقترحة |
|---|---|---|
| **بحث سريع بالسريال/العميل/الحالة** | فريق المستودع بيسأل "وين جهاز رقم كذا" 20 مرة باليوم — بدون بحث سريع، النظام عديم الفايدة عملياً | MVP (مرحلة 1) — إلزامي، مو اختياري |
| **عملية نشر/استرجاع جماعية (Bulk)** | تركيب موقع جديد = 10-15 جهاز دفعة وحدة. لو كل جهاز يحتاج شاشة منفصلة، الموظف بياخذ وقت مضاعف ويكره النظام | مرحلة 2 |
| **تتبع التكلفة الإجمالية (TCO) لكل جهاز** | تكلفة الشراء + كل الصيانات = تعرف فعلياً أي موديل يكلفك أكثر على المدى الطويل، قرار شراء مستقبلي مبني على بيانات لا تخمين | مرحلة 3 |
| **تقرير "أجهزة قاربت نهاية عمرها"** | مبني على عمر الجهاز + عدد مرات الصيانة + condition_grade — يمنعك تفاجأ بجهاز يتعطب عند عميل | مرحلة 3 |
| **قنوات تنبيه فعلية (مو بس شاشة داخل النظام)** | تنبيه بالنظام ما ينفع لو محد فاتح الشاشة. لازم يوصل إيميل/واتساب/سلاك فعلي، وإلا التنبيه نظري بس | **مرحلة 1 — هذا أهم من أي شاشة تقرير.** تنبيه ما يوصلك = ما صار |
| **سجل "مين سوى وش ولمتى"** | مو للمراقبة، للحماية القانونية — لو صار خلاف مع عميل حول جهاز مفقود، تحتاج تثبت مين سلّمه ومتى بالضبط | MVP (موجود أصلاً بتصميم Deployment، بس لازم يكون واضح بالواجهة) |
| **فلترة/تجميع حسب قطاع العميل** | ذكرت بالبداية "قطاعات العملاء مختلفة" — لو تبي تعرف أداء الأجهزة حسب قطاع (مطاعم مقابل تجزئة مثلاً)، تحتاج حقل `client_sector` من الآن، وإلا ترجع تعبيه يدوي لاحقاً لكل عميل | MVP — حقل بسيط، تكلفته صفر الآن وتكلفته عالية لو أضفته بعد سنة بيانات |
| **نسخة احتياطية/تصدير دوري** | Supabase يعمل backup تلقائي، لكن تصدير CSV دوري لك شخصياً (أسبوعي) حماية إضافية رخيصة جداً مقابل قيمتها | مرحلة 2 |

**نصيحتي الصريحة، بدون مجاملة:** أخطر بند بالجدول فوق هو **قنوات التنبيه الفعلية**. أي نظام يبني تنبيهات جوه شاشة ما حد يفتحها يومياً = فشل مضمون خلال شهرين. لو بتاخذ قرار واحد فقط من هالجدول تدخله بالمرحلة 1، خله هذا.

## 9. نقاط لسه محتاجة إجابة قبل الكود الفعلي

1. **أسماء الحقول الفعلية في Airtable** — أحتاج أشوف الجدول نفسه (تفعيل الـ connector هنا) عشان أطابق الحقول بدل ما نخمّن
2. **تنسيق الرقم التسلسلي** — هل موحّد وبدون تكرار في Airtable الحالي؟
3. **تاريخ الاشتراك** — هل كل عقد له تاريخ انتهاء ثابت، أو يتجدد تلقائياً ونحتاج نتتبع تجديدات؟
4. **قناة التنبيهات** — إيميل؟ واتساب (يحتاج API خارجي)؟ سلاك؟ حدد واحدة نبدأ فيها بالمرحلة 1
5. **قطاعات العملاء** — عطني قائمة القطاعات الفعلية اللي تتعامل معها (مطاعم، تجزئة، ...) عشان نحدد قيم حقل `client_sector` من البداية
6. **الحساسات نفسها (مو البوابة)** — ✅ مؤكد: نفس منطق البوابة (أصل فردي يرجع إلا لو تالف)
7. ~~قائمة القطع الست~~ — ✅ ستُستورد من Airtable مباشرة ضمن الاستيراد الأولي (قسم 5.5)، ما نحتاجها يدوي
8. ~~أنواع الحساسات~~ — ✅ ستُستورد من Airtable أيضاً. عمر البطارية حقل اختياري يُعبّى لاحقاً (راجع 2.8.7)
9. ~~تكلفة الشحن~~ — ✅ تُدخل يدوياً عند تعبئة المخزون مع فاتورة الشراء (راجع Purchase Invoice، قسم 2.8.7 القديم / الفواتير)
10. ~~صلاحية الفني~~ — ✅ حساب دائم، صلاحية محدودة (يشوف مهامه بس)
11. ~~إنشاء المهام~~ — ✅ المدير ينشئها يدوياً، بدون جدولة تلقائية حالياً
